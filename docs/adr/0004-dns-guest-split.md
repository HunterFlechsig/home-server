# DNS Filter has its own Guest

Household DNS must not share a Guest with the Vault and Media Server. The DNS Guest runs only the DNS Filter (AdGuard Home). The App Guest runs Vaultwarden and Jellyfin via Docker Compose. One-box-for-everything is simpler until the first Media Server experiment takes down the house internet.
