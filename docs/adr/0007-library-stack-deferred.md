# Library Stack is a later milestone

Install order is Proxmox + Tailscale + Vault + DNS Filter, then Jellyfin playing files from the Media Library, then (later) the Library Stack on this same Laptop Host. Compute is enough for Sonarr/Radarr now; operational load is not. Later agents must not add the Library Stack because “media” was mentioned.
