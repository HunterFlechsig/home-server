# Hostnames are host1, dns, and app

The Laptop Host (and its Tailscale machine) is `host1`. The DNS Guest is `dns`. The App Guest is `app`. The Vault URL is the Tailscale Serve HTTPS name on `host1`, not a public domain. Do not invent extra DNS names on the Cox Gateway.
