# Jellyfin is discovered on the LAN via mDNS

Household Consumers are not pointed at the DNS Filter, so they cannot depend on Gateway DNS. The App Guest advertises mDNS (`jellyfin.local` or equivalent) for TVs and browsers on the LAN. The App Guest IP is still recorded in inventory. Manual DNS on a TV is optional, not the plan.
